# DB_* are optional under docker compose — it has working defaults (see compose).
DB_USER=vk
DB_PASSWORD=vk
DB_NAME=vk_school_monitor
DB_HOST=db
DB_PORT=5432

# Required for VK API calls (adding students/communities, collecting posts).
VK_TOKEN=
